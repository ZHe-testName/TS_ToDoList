import React from 'react';
import { action } from '@storybook/addon-actions';
import EditableSpan from './EditableSpan';

const callback = action('Task was added to to do list');

// export default EditableSpanBasicExsample = (props: any) => {
//     return <EditableSpan />
// };